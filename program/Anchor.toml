[features]
resolution = true
skip-lint = false

[programs.devnet]
provenance = "EoWdDXF8NNnHryWFmnJazobruBvHPhZhKRR7YfrWjZ8g"

[programs.localnet]
provenance = "EoWdDXF8NNnHryWFmnJazobruBvHPhZhKRR7YfrWjZ8g"

[registry]
url = "https://api.apr.dev"

[provider]
cluster = "devnet"
wallet = "~/.config/solana/id.json"

[scripts]
test = "yarn run ts-mocha -p ./tsconfig.json -t 1000000 tests/**/*.ts"

# NOTE: run `anchor keys sync` after first build to replace the placeholder program id
# above (and the declare_id! in lib.rs) with the real keypair-derived id.
